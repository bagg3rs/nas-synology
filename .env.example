# Synology NAS Docker Compose Environment Variables

# User/Group IDs (find with: id <username>)
PUID=1000
PGID=1000

# Timezone
TZ=Europe/London

# Network Configuration
IPv4_PREFIX=192.168.0  # Your network prefix (e.g., 192.168.0.x)

# Traefik Configuration
VERSION=v2.10  # Traefik version
LOG=INFO  # Log level: DEBUG, INFO, WARN, ERROR

# Cloudflare DNS Challenge (for Let's Encrypt)
# Generate tokens: https://go-acme.github.io/lego/dns/cloudflare/
CF_DNS_API_TOKEN=your_cloudflare_dns_token
CF_ZONE_API_TOKEN=your_cloudflare_zone_token
PROVIDER=cloudflare
RESOLVER=1.1.1.1:53
EMAIL=your-email@example.com

# Traefik Dashboard Authentication
# Generate with: htpasswd -nb admin your_password
# Then escape $ as $$ for docker-compose
TRAEFIKUSER=admin:$$apr1$$hash$$here

# Domain Configuration (if using Traefik with domains)
TRAEFIK=traefik.yourdomain.com
DELUGE=deluge.yourdomain.com
PROWLARR=prowlarr.yourdomain.com
QBITTORRENT=qbit.yourdomain.com
SPEEDTEST=speedtest.yourdomain.com
HEIMDALL=heimdall.yourdomain.com
UPTIME=uptime.yourdomain.com
PORTAINER=portainer.yourdomain.com
CODE=code.yourdomain.com
